# Description

These are just my dot files. Nothing special really.

## Install instructions

Hey idiot (Huy). Don't clone this like a regular repo. Just curl the install script by copy & pasting the code below:

```sh
curl -L https://raw.githubusercontent.com/leavism/dotfiles/main/.scripts/install.sh
```

Lmao hope that works because I haven't tested it yet. That's a future Huy problem.
